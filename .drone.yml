kind: pipeline
type: docker
name: default

steps:
  - name: Build image
    image: plugins/docker
    settings:
      repo: stiforr/pyfa
      tags:
        - latest
        - build.${DRONE_BUILD_NUMBER}
      dry_run: true
